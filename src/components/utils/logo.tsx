import { cn } from '@/lib/utils';

export const logo = (
  <span
    className={cn(
      'bg-gradient-to-br from-brand/80 to-brand bg-clip-text font-bold text-transparent'
    )}
  >
    GrocEasy
  </span>
);
